- name: install packages
  hosts: frontend
  become: yes
  tasks:
  - name: install packages
    package:
      name: "{{ item }}"
      state: present
    loop:  # installing the packages one by one
     - nginx 
     - mysql
     - mongodb-mongosh
     - git
