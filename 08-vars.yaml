- name: Demo on inventory variables
  hosts: frontend
  #when we gives host as frontend then ansible will check the frontend host in inventory file also it will check that any group_vars folder is available 
  #and files under that folder are available like frontedn.yaml which consists the variables
  #also all.yaml under group_vars is the common variable file for the ansible it will execute for all hosts which is common var file for all hosts
  #connection: local
  tasks:
  - name: print information
    debug:
      msg: "I'am learning {{COURSE}}, Trainer is {{TRAINER}}, Duration is {{DURATION}}, common_key: {{common_key}}"


      